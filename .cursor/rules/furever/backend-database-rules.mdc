---
description: 宠光（PetGlow）数据库与 ORM 操作规范，适用于 models、repositories 目录下所有文件。
globs: backend/app/{models,repositories}/**/*.py
---

## 表命名规范

- 所有表名必须使用 `pet_` 前缀，单数形式（`pet_user`，不用 `pet_users`）。
- ORM 模型类名使用大驼峰，与表名对应（`PetUser` → `pet_user`）。

## 公共字段（BaseModel）

所有 ORM 模型必须继承 `app/models/base.py` 中的 `BaseModel`，该基类包含：

```python
id: int (BIGINT, AUTO_INCREMENT, PRIMARY KEY)
created_at: datetime (DEFAULT CURRENT_TIMESTAMP)
updated_at: datetime (DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP)
created_by: int (BIGINT, DEFAULT 0)
updated_by: int (BIGINT, DEFAULT 0)
is_deleted: int (TINYINT(1), DEFAULT 0)
```

禁止在子模型中重复定义上述字段。

## Repository 接口规范

- 每个业务实体必须在 `app/repositories/base.py` 中定义抽象接口（ABC）。
- Mock 实现（`repositories/mock/`）和 DB 实现（`repositories/db/`）必须实现相同接口，方法签名完全一致。
- Repository 方法命名约定：
  - `find_by_id(id: int)` → 按主键查询单条（过滤 is_deleted）
  - `find_list(...)` → 查询列表（必须支持分页参数 page/page_size，返回 tuple[list, int]）
  - `create(data: XxxCreateReq, user_id: int)` → 创建记录，自动填充 created_by/updated_by
  - `update(id: int, data: dict, user_id: int)` → 更新记录，自动填充 updated_by
  - `soft_delete(id: int, user_id: int)` → 逻辑删除，设置 is_deleted=1

## 查询规范

- 所有查询必须过滤 `is_deleted=0`，禁止查询已删除记录（除特殊审计场景）。
- 分页查询必须同时返回总数（`COUNT(*)`），用于前端分页展示。
- 禁止在 Repository 中使用原生 SQL 字符串拼接，必须使用 ORM 查询构造器或参数化查询。
- 批量查询使用 `IN` 而非循环单条查询。

## Mock 实现规范

- Mock 数据存储在模块级别的 `list` 或 `dict` 变量中（进程内共享，重启重置）。
- Mock 实现必须模拟真实行为：分页截取、is_deleted 过滤、唯一约束检查。
- Mock 中的 id 自增使用模块级计数器（`_next_id`）。
- Mock 数据必须包含完整公共字段（id、created_at、updated_at、created_by、updated_by、is_deleted）。
- 预置测试数据：至少 2 个用户、5 条帖子、若干评论和通知，数据内容与宠物主题相关。
